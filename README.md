# Idealist Auth Callback

Static callback page for Idealist authentication emails.

- Mobile devices are redirected to `idealist://auth/confirm` or `idealist://auth/reset-password`.
- Desktop browsers show a confirmation message without exposing auth parameters in the address bar.

This repository contains no app source code or secrets.
